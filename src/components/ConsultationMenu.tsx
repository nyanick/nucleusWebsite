import React from "react";

interface ConsultationMenuProps { }

const ConsultationMenu: React.FC<ConsultationMenuProps> = ({ }) => {
    return (
        <header
            className='bg-blue-500 sticky top-0 w-screen py-3 z-[101]'>
        </header>
    )
}

export default ConsultationMenu